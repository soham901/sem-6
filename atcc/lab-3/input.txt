#include<stdio.h>

int main() {
    FILE *f1, *f2;
    char c, p;
    // f1 = fopen("input.txt", "r");
    // f2 = fopen("temp.txt", "w");
    /* while((c=fgetc(f1) != EOF)) {
        if (c == '/') {
            c = fgetc(f1);
            if (c == '/') {
            }
            else if(c=='*') {
                p = c;
                c = fgetc(f1);
            } while (p != '*' || c != '/');
            printf("\nMultiline comment ignored\n");
        }
        else {
            ungetc(c, f1);
        }
    } */
    return 0;
}
